'client url'

import { collectSegments } from "next/dist/build/segment-config/app/app-segments";
import styles from "./quadro.module.css";

const cadeiras = map.src('../public/filme.json');

export default function Quadro({url, titulo, autor, ano}){
    return (
        <div>
            
        </div>
    );
}